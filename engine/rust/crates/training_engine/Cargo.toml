[package]
name = "training_engine"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
storage = { path = "../storage" }
tokio = { version = "1.0", features = ["full"] }
uuid = { version = "1.0", features = ["v4"] }

[dev-dependencies]
tempfile = "3.20"
